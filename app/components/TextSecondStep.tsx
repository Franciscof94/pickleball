import { outfit } from "../home-page"

export const TextSecondStep = () => {
    return (
        <p className={`text-base text-blue-text font-medium m-0 pt-3 ${outfit.className}`}>
            To watch your reservations enter your contact name.
        </p>
    )
}